# 手机 ToMemo 最终验收

将 `ToMemo-Acceptance-v1.0.json` 导入手机 ToMemo，确认：

- 出现“Web Editor 验收”分类和正确颜色。
- 出现普通内容、空正文、动态变量三条 Memo。
- 普通正文完整，空正文可显示。
- `{{CLIPBOARD}}` 与 `{{CURSOR}}` 在 ToMemo 中按预期工作。

## 验收结果

2026-07-12 已在真实手机 ToMemo 中导入成功：新分类和三条 Memo 正常出现，既有数据未受干扰。ToMemo 导入界面同时确认：相同 ID 的数据会被覆盖，不同 ID 的数据会独立新增。

状态：**通过**。
