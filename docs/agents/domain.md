# Domain documentation

This repository uses a single domain context.

Before changing behavior, read:

- `CONTEXT.md` for the shared vocabulary and known ToMemo invariants.
- Relevant records in `docs/adr/` for architectural decisions.

Use the glossary's exact terms in code, tests, specifications, and tickets. Surface conflicts with an ADR rather than silently overriding it.

